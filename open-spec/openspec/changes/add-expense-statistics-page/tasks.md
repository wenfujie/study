## 1. 页面与数据结构

- [x] 1.1 新增花销统计页面组件与路由入口（如项目已有路由则接入现有导航）
- [x] 1.2 定义表格行数据结构，包含 `expenseItem` 与 `expenseAmount` 两个字段
- [x] 1.3 渲染两列表格，并为两列单元格提供输入框

## 2. 输入约束与交互

- [x] 2.1 实现花销项输入更新逻辑，保证可编辑
- [x] 2.2 实现金额输入校验逻辑，仅允许数字且最多两位小数
- [x] 2.3 处理金额输入边界场景（空值、小数点中间态、非法字符）

## 3. 总金额计算与展示

- [x] 3.1 基于金额列实时计算合计 `amount`（派生计算，不冗余存储）
- [x] 3.2 在表格下方展示总金额并确保输入变更后即时刷新
- [x] 3.3 补充/更新测试，覆盖金额校验与实时合计关键场景
